import { frictionPoints } from '@/lib/site-data';import Card from './ui/Card';import NodeIcon from './ui/NodeIcon';import Section from './ui/Section';
export default function ProblemSection(){return <Section><div className="mx-auto grid max-w-container gap-8 px-5 md:px-8 lg:grid-cols-2 lg:px-12"><div><p className="text-sm text-muted">The quiet cost of messy operations</p><h2 className="mt-3 font-heading text-4xl text-evergreen">Work slows down when systems stop matching reality.</h2><p className="mt-4 text-ink">Most teams do not need more tools. They need the right parts of the system cleaned up, connected, automated, or made visible.</p></div><div className="grid gap-3 sm:grid-cols-2">{frictionPoints.map(i=><Card key={i.title}><NodeIcon/><h3 className="mt-3 text-base text-ink">{i.title}</h3></Card>)}</div></div></Section>}
