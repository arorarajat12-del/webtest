import Card from './ui/Card';import Section from './ui/Section';
const data=[['Focused, not bloated','Small scopes, clear priorities, and practical fixes instead of oversized transformation projects.'],['Systems-first','CRM, workflows, reporting, and operations are considered together — not as disconnected tasks.'],['Founder-led','You work directly with someone who understands both strategy and implementation.'],['Tool-aware, not tool-obsessed','HubSpot, Zapier, AI, and automation tools only matter if they support the real process.'],['Built for clarity','The final system should make work easier for the people using it, not harder to understand.']];
export default function DifferenceSection(){return <Section><div className="mx-auto max-w-container px-5 md:px-8 lg:px-12"><h2 className="font-heading text-4xl text-evergreen">Practical enough to build. Strategic enough to matter.</h2><p className="mt-4 text-ink">Tinyvation is not built around bloated roadmaps or generic advice.</p><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-5">{data.map(d=><Card key={d[0]} className="h-full"><h3 className="text-base text-evergreen">{d[0]}</h3><p className="mt-2 text-sm text-muted">{d[1]}</p></Card>)}</div></div></Section>}
