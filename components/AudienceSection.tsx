import { audience } from '@/lib/site-data';import Section from './ui/Section';
export default function AudienceSection(){return <Section id="about"><div className="mx-auto max-w-container px-5 md:px-8 lg:px-12"><h2 className="font-heading text-4xl text-evergreen">Built for small teams doing important work with imperfect systems.</h2><p className="mt-4 text-ink">Tinyvation is a good fit when the work matters, the team is stretched, and the systems are making things harder than they need to be.</p><div className="mt-6 flex flex-wrap gap-3">{audience.map(a=><span key={a} className="rounded-full border border-border bg-card px-4 py-2 text-sm text-ink">{a}</span>)}</div><p className="mt-6 text-sm text-muted">You do not need a perfect brief. A messy workflow is enough to start.</p></div></Section>}
