import Section from './ui/Section'; const steps=[['Map the friction','We look at how work moves across people, tools, forms, spreadsheets, CRM records, emails, and reports.'],['Find the highest-leverage fix','We identify the smallest practical fix that creates the most movement without adding unnecessary complexity.'],['Build the system','We clean, connect, automate, or rebuild the workflow that is slowing the team down.'],['Make it usable','The final setup should be clear enough for the team to understand, use, and maintain.']];
export default function ProcessSection(){return <Section id="process"><div className="mx-auto max-w-container px-5 md:px-8 lg:px-12"><h2 className="font-heading text-4xl text-evergreen">A small process for better systems.</h2><p className="mt-3 text-ink">The work starts by understanding how things actually move today — then fixing the part that creates the most operational lift.</p><div className="mt-8 grid gap-4 lg:grid-cols-4">{steps.map((s,i)=><div key={s[0]} className="rounded-2xl border border-border bg-card p-5"><p className="text-sm text-teal">0{i+1}</p><h3 className="mt-2 text-lg text-evergreen">{s[0]}</h3><p className="mt-2 text-sm text-muted">{s[1]}</p></div>)}</div></div></Section>}
