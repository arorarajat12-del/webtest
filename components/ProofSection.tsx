import { proofCards } from '@/lib/site-data';import Card from './ui/Card';import Section from './ui/Section';
export default function ProofSection(){return <Section id="proof"><div className="mx-auto max-w-container px-5 md:px-8 lg:px-12"><p className="text-sm text-muted">Relevant experience</p><h2 className="mt-3 font-heading text-4xl text-evergreen">Built from real systems work, not theory.</h2><p className="mt-3 text-ink">Tinyvation is shaped by hands-on experience designing and improving systems across CRM, automation, reporting, lifecycle tracking, proposal workflows, and internal operations.</p><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{proofCards.map(c=><Card key={c.title}><h3 className="text-lg text-evergreen">{c.title}</h3><p className="mt-2 text-muted">{c.description}</p></Card>)}</div></div></Section>}
