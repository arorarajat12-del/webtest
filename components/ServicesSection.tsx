import { services } from '@/lib/site-data';import Button from './ui/Button';import Card from './ui/Card';import Section from './ui/Section';
export default function ServicesSection(){return <Section id="services"><div className="mx-auto max-w-container px-5 md:px-8 lg:px-12"><h2 className="font-heading text-4xl text-evergreen">Focused systems support for the parts of work that keep getting stuck.</h2><p className="mt-3 text-ink">Start with a small audit, a focused cleanup, a workflow build, or ongoing systems support.</p><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{services.map(s=><Card key={s.name}><h3 className="text-xl text-evergreen">{s.name}</h3><p className="mt-2 text-muted">{s.description}</p><p className="mt-3 text-sm text-ink"><strong>Best for:</strong> {s.bestFor}</p><a className="mt-4 inline-block text-sm text-evergreen underline" href="#contact">Learn more</a></Card>)}</div><div className="mt-8 rounded-2xl border border-border bg-softgreen p-6"><p className="text-ink">Not sure where to start?</p><div className="mt-3"><Button href="#contact">Request an Audit</Button></div></div></div></Section>}
