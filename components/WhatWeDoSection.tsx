import { capabilities } from '@/lib/site-data';import Card from './ui/Card';import Section from './ui/Section';
export default function WhatWeDoSection(){return <Section><div className="mx-auto max-w-container px-5 md:px-8 lg:px-12"><h2 className="font-heading text-4xl text-evergreen">We find the small fix that creates the most movement.</h2><p className="mt-4 max-w-3xl text-ink">Tinyvation works across CRM, automation, reporting, lifecycle tracking, and internal workflows to turn messy operations into cleaner systems your team can actually use.</p><div className="mt-8 grid gap-4 md:grid-cols-3">{capabilities.map(c=><Card key={c.title}><h3 className="text-xl font-medium text-evergreen">{c.title}</h3><p className="mt-3 text-muted">{c.description}</p></Card>)}</div></div></Section>}
